set terminal png size 800,500 enhanced background rgb 'white
set output 'curve.png'

set title 'Fusion'
set xrange [0:]
set yrange [0:]

f(n) = n * log(n)/log(2)
g(n) = n * log(n)
h(n) = 2* n * log(n)
plot 'output.dat' using 1:2 smooth bezier w lines title "tests",\
 f(x) w lines title "nlog2(n)",\
 g(x) w lines title "nlog(n)",\
 h(x) w lines title "2nlog(n)"
 